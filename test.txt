#LANG_CLASSICAL;
#include <fichier>;

func testValue() (int a) {
    return 1;
}

def float a = 1.456456;

if (testValue()) {
    def int b = 1;
} else if (not testValue()) {
    def int b = 2;
} else {
    def int b = 3;
}